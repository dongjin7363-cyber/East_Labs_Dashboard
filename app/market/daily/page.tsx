import { PageHeader } from "@/components/PageHeader";

export default function MarketDailyPage() {
  return (
    <section>
      <PageHeader title="Daily Stock" />
      <div className="panel" style={{ padding: "48px 0", textAlign: "center", color: "var(--muted)", fontSize: 14 }}>
        준비 중
      </div>
    </section>
  );
}
