# Export Items Seed Notes

- `data/export_items.csv` was seeded from the currently reachable `export_items` whitelist.
- The source table used `item_name` and star ratings; the CSV normalizes those to `name` and numeric `importance`.
- Before adding more rows, confirm the exact Excel `sheet_name` from the monthly workbook. Do not add guessed sheet names.
